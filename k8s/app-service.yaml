apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: attendance
spec:
  selector:
    app: attendance-service
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP

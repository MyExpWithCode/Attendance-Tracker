apiVersion: v1
kind: Secret
metadata:
  name: attendance-db-secret
  namespace: attendance
type: Opaque
stringData:
  # Single source of truth for the database password.
  # Used by the app (DB_PASSWORD) and the Postgres container (POSTGRES_PASSWORD).
  DB_PASSWORD: "Pa55w0rd!"
